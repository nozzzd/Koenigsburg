# 2026-07-23 — Königsburg portal session

## Project & workflow
- Repo: `/home/claw/projects/koenigsburg/source` (NOT `/workspace`). Next.js App Router, TS strict, Tailwind v4, Supabase, Discord OAuth. Remote `github.com/nozzzd/Koenigsburg`, default branch `main`, `gh` authed as `nozzzd`.
- **Work directly on `main`; never create branches/PRs.** Leave finished work as UNCOMMITTED changes — owner commits + syncs from their own Git client. Do NOT commit or push. Codified in `AGENTS.md`.
- `main` auto-deploys to prod via Vercel → only hand back changes that build cleanly. Always run `npx tsc --noEmit`, `npm run lint`/eslint, `npm run build` before handing back.
- **Never run Supabase migrations or touch Supabase.** I write numbered SQL files in `supabase/`; the USER runs them. Never touch secrets/`.env.local`.
- Newer Next.js: dynamic route `params` are `Promise<{id}>` and must be awaited. Read `node_modules/next/dist/docs/` before Next.js changes.
- Pre-existing unrelated lint errors in `funnel/page.tsx` and `ThemeToggle.tsx` — leave untouched.
- **No em dashes anywhere in site text — ever.** Use spaced hyphens or rephrase. (Already stripped all 233 site-wide; excluded `local-only/` and `media/inbound/`.)
- User prefers: act, don't over-ask; but DO ask when genuinely unclear/architecture-defining. Wants fewer approval prompts for routine low-risk work.

## Build planner (done, live on main via earlier PR #5 + later work)
- `lib/builds.ts` (allocation engine; `getBuildOverview()` gives `shortfalls`, wrapped in React `cache()`; items now sorted by largest outstanding gap first), `lib/litematica.ts` (parser + `slugToItemId` + new `formatItemName()` sentence-caser), `actions/builds.ts` (admin-guarded CRUD).
- Admin pages `app/portal/admin/builds/{page,[id]/page}.tsx`; citizen read-only `app/portal/builds/{layout,page,[id]/page}.tsx`; nav links; `MostNeededWidget` on dashboard.
- Feature A/B/C migration `supabase/014_build_assignees_and_files.sql` (assignee cols + files table + `build-files` public bucket). Assignees informational only (team XOR player). Litematica uploads: `.litematic/.schem/.schematic/.nbt`, 25MB cap; `next.config.ts` bodySizeLimit 26mb.
- **Fake-item guard:** `lib/minecraft-items.ts` = real MC item id registry, pinned to **pc 1.21.11** (1,504 ids), `isRealMinecraftItem()`. `addBuildItem` rejects fake `minecraft:` ids; `importMaterials` drops+summarizes; modded namespaces allowed. Datalist autocomplete on add-item id field. NOTE: does NOT retro-clean pre-existing phantom rows (e.g. `oak`, `oak wood`) — user must delete those manually.
- **STILL PENDING (user must run):** `supabase/013_build_projects.sql`, `supabase/014_...sql`, create `build-files` bucket if INSERT blocked.

## Minecraft-styled inventory/UI (done)
- Icons from `mc.nerothe.com` — `https://mc.nerothe.com/img/1.21/minecraft_<id>.png` (3D blocks, flat items, clean 404 → onError fallback). `components/ItemIcon.tsx` (letter-tile fallback).
- `components/InventoryGrid.tsx` — MC slot grid; tooltip is a single viewport-fixed element measured+clamped in JS (never clips any edge), flips below near top; slots keyboard-focusable.
- `app/globals.css` — `.mc-panel/.mc-slot/.mc-count/.mc-tooltip/.pixelated`; VT323 pixel font `--font-pixel` (in `app/layout.tsx`). Light mode inventory = authentic MC grey GUI (`#c6c6c6` panel, `#8b8b8b` slots).
- Added `color-scheme: dark`/`light` on roots so number-steppers/scrollbars theme correctly (dark box+white arrows in dark, white+dark in light).
- `components/RemoveItemButton.tsx` — optimistic delete (row fades/slides out instantly via useTransition, restores on error). Used for build-item AND file-delete buttons.

## Map atlas (NEW this session — big feature)
- Migration **`supabase/015_map_atlas.sql`** (tables `map_layers`, `map_markers`, `map_claims`; RLS on, NO policies — server-only service-role access). **USER MUST RUN IT.**
- `lib/atlas.ts` — types + `getAtlas(canSeeSecret)`; also now holds `DisplayTile` type (moved from deleted `MapCanvas.tsx`). **Secret filtering happens server-side** — secret layers/markers/claims stripped before render so non-active-citizens never receive them (inspect-element safe). `canSeeSecret` = active citizen.
- `actions/atlas.ts` — admin-guarded CRUD for layers/markers/claims (`requireAdmin` = role admin + status active). Coord clamp ±30M.
- `components/map/atlas/AtlasMap.tsx` (main; big), `MarkerGlyph.tsx`, `symbols.tsx` (26 lucide map symbols). Coord system: `CELL=256`, `SCALE=0.5` px/block; zoom 0.03–24. Tiles rendered in SCREEN-space (same path as markers — a zero-size transform layer failed to paint, that was the "map doesn't render" bug).
- Features: fullscreen (Fullscreen API), deep zoom (wheel via NATIVE non-passive listener so page doesn't scroll), live X/Z hover readout, MC crosshair (`mix-blend-mode:difference`, `cursor:none`), hideable sidebar (Countries/Groups + eye toggles + Fill/Names global toggles), two-layer model (country=colour+claims+landmarks / group=generic pins), markers (waypoint/symbol/MC-item — item now NO background), drag-to-move, polygon land claims (filled+outlined at 0.2 opacity, fill toggleable), per-layer AND per-marker/claim Secret flag, inspector panel for editing.
- `app/map/page.tsx` renders `AtlasMap` (loads atlas + session, gates secrets). Deleted old `components/map/MapCanvas.tsx` (folded `DisplayTile` into `lib/atlas.ts`; repointed `DownloadMapButton`).
- `lib/xaero/render.ts` — water toned down (detect water, flatten relief highlight, dim to 70% `WATER_DIM`). NOTE: renderer runs at UPLOAD time and bakes PNGs; existing stored tiles are NOT re-rendered — new look only appears on re-upload.

## OPEN / PENDING map requests (user asked, NOT yet done — do next)
1. **Hover popup on markers** showing important data.
2. **Admins can write descriptions** for markers (needs a `description` column on `map_markers` in a new migration or extend 015 if unrun — but 015 likely already handed over; add `supabase/016_*.sql` for the column).
3. **Redesign the layers sidebar** — user finds it "not good / overwhelming"; make it more user-friendly, simpler, easier. Review and improve UX.

## UI patterns to reuse
- `useActionState` + `ActionState` (`{error}|null`) from `lib/forms.ts`; `SubmitButton`; UI helpers from `components/ui.tsx` (`Panel`, `GoldDivider`, `cardLinkClass`, `inputClass`, `labelClass`, `navButtonClass`, `ErrorBanner`, button classes). Admin guard: `getSessionPlayer()` + role/status. Dark medieval/royal-gold theme, light-mode via `[data-theme="light"]`.
